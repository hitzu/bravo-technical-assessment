meta {
  name: 01-list (active)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/countries
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

