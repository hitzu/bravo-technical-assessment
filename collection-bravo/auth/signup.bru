meta {
  name: signup
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "tenantId": "{{tenantId}}",
    "email": "admin@reparadora.com",
    "fullName": "Admin reparador uno",
    "role": "ADMIN",
    "status": "ACTIVE"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
