meta {
  name: 18-create (agent, risk eval ES)
  type: http
  seq: 18
}

post {
  url: {{base_url}}/applications
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{agent_token}}
}

body:json {
  {
    "countryId": "{{countryId_es}}",
    "fullName": "Juan Pérez",
    "documentId": "XEXX010101000",
    "monthlyIncome": 2500000,
    "requestedAmount": 1000
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
