meta {
  name: 10-get by id (agent, happy - mine)
  type: http
  seq: 10
}

get {
  url: {{base_url}}/applications/{{applicationId}}
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
