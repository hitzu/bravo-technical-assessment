meta {
  name: 06-list (admin - all in tenant)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/applications?page=1&pageSize=20
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

